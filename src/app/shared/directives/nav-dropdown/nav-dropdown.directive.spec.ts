import { NavDropdownToggleDirective } from './nav-dropdown.directive';

describe('NavDropdownToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new NavDropdownToggleDirective(1);
    expect(directive).toBeTruthy();
  });
});
